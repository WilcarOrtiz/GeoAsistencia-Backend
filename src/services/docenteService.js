const { Docente, Asignatura } = require("../models");

async function asignaturaConGrupos(id_docente) {
  //  servicio para obtener los asignatura de un docente con sus grupos
}

module.exports = {
  asignaturaConGrupos,
};
